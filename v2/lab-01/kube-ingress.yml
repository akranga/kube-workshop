apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: web-ingress
spec:
  rules:
  - host: wordsmith.app.<fqdn-of-your-cluster>
    http:
      paths:
      - path: /
        backend:
          serviceName: web
          servicePort: 80
